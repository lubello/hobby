{% extends 'base.html.twig' %}

{% block title %}Departement{% endblock %}

{% block body %}
    <h1>Departement</h1>
<div class="mx-autooooo col-lg-6 col-10">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ departement.nom }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ departement.region }}</td>
            </tr>
        </tbody>
    </table>

    <div>
        <a class="btn btn-sm btn-info" href="{{ path('departement_index') }}">back to list</a>
        <a class="btn btn-sm btn-primary" href="{{ path('departement_edit', {'id': departement.id}) }}">edit</a>
        {{ include('departement/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
