<h3 hidden>Ma sidebar {{ param1 }}</h3>

{# {{ dump(posts) }} #}


{% for item in posts %}
<li>
    <a href="{{ item.href }}">
        <i class="tim-icons icon-app" ></i>
        <p>{{ item.name }}</p>
    </a>
</li>
{% endfor %}

